1. To create a new branch: 'git checkout -b branch-name
2. To publish the branch to GitHub: 'git push origin branch-name
3. To switch to the branch on GitHub, ensure the branch is pushed and then use 'git checkout branch-name.
